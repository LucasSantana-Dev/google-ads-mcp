"""Google Ads MCP server — wraps the Google Ads API for MCP clients."""

__version__ = "0.1.0"

# Google Ads API version this server is pinned to. Bump deliberately; review breaking
# changes in the migration guide before raising.
GOOGLE_ADS_API_VERSION = "v24"
